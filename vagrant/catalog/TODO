Reuqired field
Description to be Long text field instead of short text filed
Add images to items if available, otherwise add default image according to its catagory
Make catagories added by default
Login page
